{
  "name": "chatapp-local-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Temporary local backend for emulator testing. Not used in production.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "socket.io": "^4.8.1"
  }
}
